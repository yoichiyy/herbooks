name: counter
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+4

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  path_provider: ^2.0.9
  firebase_core: ^1.17.0
  cloud_firestore: ^3.4.6
  provider: ^6.0.3
  firebase_auth: ^3.11.2

  flutter_datetime_picker: ^1.5.1
  confetti: ^0.7.0

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.3.2

  flutter_launcher_icons: ^0.10.0

flutter:
  uses-material-design: true


flutter_icons:
  android: true 
  ios: true
  remove_alpha_ios: true
  image_path: "lib/assets/appstore.png"
  
